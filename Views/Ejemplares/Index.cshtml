@model BookCore.ViewModels.EjemplarIndiceViewModel

@{
    ViewData["Title"] = "Ejemplares";
}

<div class="contenedor-pagina">
    <div class="tarjeta-panel">

        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="mb-1">
                    Ejemplares
                </h1>

                <p class="text-muted mb-0">
                    Administra las copias físicas de los libros.
                </p>
            </div>

            <a asp-action="Crear"
               asp-route-libroId="@Model.LibroId"
               class="btn btn-primary">
                Nuevo ejemplar
            </a>
        </div>

        <form asp-action="Index"
              method="get"
              class="row g-3 mb-4">

            <div class="col-md-4">
                <label asp-for="Busqueda"
                       class="form-label">
                    Búsqueda
                </label>

                <input asp-for="Busqueda"
                       class="form-control"
                       placeholder="Código, libro o ubicación" />
            </div>

            <div class="col-md-3">
                <label asp-for="LibroId"
                       class="form-label">
                    Libro
                </label>

                <select asp-for="LibroId"
                        asp-items="Model.Libros"
                        class="form-select">

                    <option value="">
                        Todos los libros
                    </option>

                </select>
            </div>

            <div class="col-md-2">
                <label asp-for="Estado"
                       class="form-label">
                    Estado
                </label>

                <select asp-for="Estado"
                        asp-items="Model.Estados"
                        class="form-select">

                    <option value="">
                        Todos
                    </option>

                </select>
            </div>

            <div class="col-md-1 d-flex align-items-end">
                <div class="form-check mb-2">
                    <input asp-for="SoloActivos"
                           class="form-check-input" />

                    <label asp-for="SoloActivos"
                           class="form-check-label">
                        Activos
                    </label>
                </div>
            </div>

            <div class="col-md-2 d-flex align-items-end gap-2">
                <button type="submit"
                        class="btn btn-outline-primary">
                    Buscar
                </button>

                <a asp-action="Index"
                   class="btn btn-outline-secondary">
                    Limpiar
                </a>
            </div>

        </form>

        @if (!Model.Ejemplares.Any())
        {
            <div class="alert alert-info mb-0">
                No se encontraron ejemplares.
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead>
                        <tr>
                            <th>Código <i class="bi bi-arrow-down-up orden-columna"></i></th>
                            <th>Libro <i class="bi bi-arrow-down-up orden-columna"></i></th>
                            <th class="col-oculta-angosta">Ubicación <i class="bi bi-arrow-down-up orden-columna"></i></th>
                            <th>Estado <i class="bi bi-arrow-down-up orden-columna"></i></th>
                            <th class="col-oculta-angosta">Registro <i class="bi bi-arrow-down-up orden-columna"></i></th>
                            <th>Activo <i class="bi bi-arrow-down-up orden-columna"></i></th>
                            <th class="text-end">Acciones</th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var ejemplar in Model.Ejemplares)
                        {
                            <tr>
                                <td>
                                    <span class="pastilla-codigo">
                                        @ejemplar.CodigoInterno
                                    </span>
                                </td>

                                <td>
                                    <div class="libro-celda">
                                        <span class="icono-libro-mini c2">
                                            <i class="bi bi-book"></i>
                                        </span>
                                        @ejemplar.LibroTitulo
                                    </div>
                                </td>

                                <td class="col-oculta-angosta">
                                    @if (string.IsNullOrWhiteSpace(
                                        ejemplar.Ubicacion))
                                    {
                                        <span class="text-muted">
                                            No indicada
                                        </span>
                                    }
                                    else
                                    {
                                        @ejemplar.Ubicacion
                                    }
                                </td>

                                <td>
                                    @switch (ejemplar.Estado)
                                    {
                                        case "Disponible":
                                            <span class="estado-punto activo">
                                                Disponible
                                            </span>
                                            break;

                                        case "Prestado":
                                            <span class="estado-punto prestado">
                                                Prestado
                                            </span>
                                            break;

                                        case "Dañado":
                                            <span class="estado-punto danado">
                                                Dañado
                                            </span>
                                            break;

                                        default:
                                            <span class="estado-punto inactivo">
                                                Baja
                                            </span>
                                            break;
                                    }
                                </td>

                                <td class="col-oculta-angosta">
                                    <span class="fecha-con-icono">
                                        <i class="bi bi-calendar3"></i>
                                        @(ejemplar.FechaIngreso.ToString(
                                            "dd/MM/yyyy"))
                                    </span>
                                </td>

                                <td>
                                    @if (ejemplar.Activo)
                                    {
                                        <span class="estado-punto activo">
                                            Sí
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="estado-punto inactivo">
                                            No
                                        </span>
                                    }
                                </td>

                                <td>
                                    <div class="acciones-tabla">

                                        <a asp-action="Detalle"
                                           asp-route-id="@ejemplar.EjemplarId"
                                           class="accion-icono ver"
                                           title="Ver detalle">
                                            <i class="bi bi-eye"></i>
                                        </a>

                                        <a asp-action="Editar"
                                           asp-route-id="@ejemplar.EjemplarId"
                                           class="accion-icono editar"
                                           title="Editar">
                                            <i class="bi bi-pencil"></i>
                                        </a>

                                        @if (ejemplar.Activo)
                                        {
                                            <a asp-action="Eliminar"
                                               asp-route-id="@ejemplar.EjemplarId"
                                               class="accion-icono eliminar"
                                               title="Eliminar">
                                                <i class="bi bi-trash"></i>
                                            </a>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }

    </div>
</div>