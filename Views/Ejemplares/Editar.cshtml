@model BookCore.ViewModels.EjemplarFormularioViewModel

@{
    ViewData["Title"] = "Editar ejemplar";
}

<div class="contenedor-pagina">
    <div class="tarjeta-panel">

        <div class="mb-4">
            <h1>Editar ejemplar</h1>

            <p class="text-muted">
                Modifica la información de esta copia física.
            </p>
        </div>

        <form asp-action="Editar"
              asp-route-id="@Model.EjemplarId"
              method="post">

            <partial name="_Formulario"
                     model="Model" />

            <div class="d-flex gap-2 mt-4">
                <button type="submit"
                        class="btn btn-primary">
                    Guardar cambios
                </button>

                <a asp-action="Index"
                   asp-route-libroId="@Model.LibroId"
                   class="btn btn-outline-secondary">
                    Cancelar
                </a>
            </div>
        </form>

    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}